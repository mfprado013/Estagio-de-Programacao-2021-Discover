// Operador de Dtrings (String Operator)

// Comparison (comparação)
console.log('a' == 'b')
//concatenation (concatenação)
//Retorna a união de duas Strings
let alpha = 'alpha'
console.log( alpha += 'bet')

// Aqui estamos concatenando as Strings, mesmo que colocamos numeros no meio, vamos conseguir somente a concatenação